#if you already have a terraform state config file for your/the environment, you can remove/delete this whole file.
key            = "mybucket"
dynamodb_table = "mybucket-bucket"
bucket         = "mybucket"
region         = "us-east-1"
